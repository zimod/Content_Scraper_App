a Node.js command line application that goes to an ecommerce site to get the latest prices and save them to a spreadsheet (CSV format)
The saved CSV file can be accessed inside the generated data folder
use npm install for dependencies, and then use npm start to run the app
